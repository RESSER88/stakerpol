/** Dane przeniesione automatycznie z zapytania do formularza nowej oferty. */
export interface OfferPrefill {
  leadId?: string;
  nazwa?: string;
  telefon?: string;
  email?: string;
  notatka?: string;
}
